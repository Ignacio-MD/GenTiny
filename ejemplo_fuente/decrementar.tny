{ Programa de ejemplo
  en lenguaje TINY -
  Se decrece un valor dado
  por el usuario hasta que
  sea igual a cero
}
read x; { Introducir un Interger }
if 0 < x then { Se ejecuta el resto del programa si x > 0 }
  y := x; { Se guarda el valor original de x en y } 
  repeat
    dec x; { El valor de x decrece en uno } 
    write x { Se imprime el nuevo valor de x }
  until x = 0;
  write y  { Se imprime y al final }
end
